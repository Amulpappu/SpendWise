services:
  - type: web
    name: spendwise-web
    env: node
    rootDir: web
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
